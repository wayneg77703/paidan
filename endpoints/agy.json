{
  "schema_version": "1.0.0",
  "name": "agy",
  "detect": {
    "bin": "agy",
    "version_args": [
      "--version"
    ],
    "version_re": "^([0-9]+\\.[0-9]+\\.[0-9]+)",
    "detect_notes": "`agy --version` prints a bare semver (\"1.2.0\", live 2026-09-10). On this machine agy is a single native agy.exe directly on PATH (no npm shim, no script wrapper), so detect.npm_exe/npm_entry do not apply: the PATH scan resolves the .exe and spawns it without a shell. The binary self-updates in place (an in-place 1.1.27 -> 1.1.28 update was observed in the 2026-09-09 probe report; this record is calibrated on 1.2.0); version drift policy applies — re-run probes after upgrades."
  },
  "command": {
    "argv": [
      "{bin}",
      "-p",
      "{prompt}",
      "--output-format",
      "stream-json",
      "--print-timeout",
      "30m"
    ],
    "prompt_delivery": "argv",
    "mode_args": {
      "read-only": [
        "--mode",
        "plan"
      ],
      "workspace-write": [
        "--mode",
        "accept-edits"
      ],
      "unattended": [
        "--mode",
        "accept-edits"
      ]
    },
    "mode_args_notes": "agy has no --permission-mode; the agent-execution axis is `--mode` (help 1.2.0 lists accept-edits|plan; no flag = default). Live 2026-09-10 native zero-copy probe (raw captures distilled from operator scratch, deleted 2026-09-13): plan headless auto-denies write_file even with ZERO permission rules (t2a: file absent, result.denied_actions[write_file], exit 0) — a true enforced read-only tier. accept-edits auto-approves write_file when no rule governs it (t2c). [Superseded 2026-09-10, agy 1.2.0: the 1.1.28-era write_file(<root>) scoping story below no longer holds — write_to_file is now confined to brain/<conversation>/ artifacts regardless of rules, and deliverable writes ride run_command (shell), which starts in agy's own scratch dir; see native_preflight.notes and README. Write scoping via settings rules is therefore NOT a live mechanism for deliverables.] paidan never writes the settings file (invariant 4); the rules are user-maintained native config. unattended shares accept-edits; its shell.exec requirement rides the native command(*) allow rule (verified t6c). --sandbox is deliberately absent: on Windows 1.2.0 it adds an unsandboxed(*) gate that headless can never grant (t6b auto-deny) and its readwrite mount config rejects drive-stripped rule paths — the native permission engine is the enforcement layer.",
    "model_arg": [
      "--model",
      "{model}"
    ],
    "add_dir_arg": [
      "--add-dir",
      "{dir}"
    ],
    "argv_notes": "Prompt rides argv (-p <prompt>) like the suite adapter (prompt_transport=argv); there is no single-shot stdin text mode (--input-format stream-json is a multi-turn NDJSON protocol, out of v0 scope). Windows command-line budget applies to the prompt bytes (suite cap 26 KiB). --print-timeout is always passed: native default is 5m, window 30s..120m; 30m preserves the historical operational envelope. 1.2.0 timeout semantics: partial output + exit 0 + status SUCCESS + stderr marker, surfaced as the print-timeout-partial-output refusal signal. Native config (~/.gemini) decides auth and default model; paidan never stages or overrides beyond the flags above. [2026-09-18 1.2.5: P1-write=pass, P2-readonly-refusal=indeterminate, P3-resume=pass. Native settings unchanged. Isolated paidan homes used; model/effort followed the user-selected combination.]",
    "cwd_arg": null,
    "env": {
      "AGY_CLI_DISABLE_AUTO_UPDATE": "true",
      "_env_notes": "AGY_CLI_DISABLE_AUTO_UPDATE=true is pinned (same as the suite): without it, headless runs still touched the native updater (update_status.json flipped to 'Update failed, please install from website', last_check.timestamp refreshed; live 2026-09-10) even though antigravity-cli/settings.json carries update.mode=none; with the env var, updater files stay untouched (verified). HOME/USERPROFILE are NOT set: agy runs against the user's real ~/.gemini (zero-copy native home; Google OAuth lives in the OS keyring, unaffected by HOME isolation). Paidan never stages or copies agy config/credentials."
    },
    "verified_at": "2026-09-18",
    "version": "1.2.5",
    "prompt_cwd_hint": true
  },
  "permission": {
    "fs.read": {
      "status": "supported",
      "via": "view_file requires a native read_file allow rule (request-review default auto-denies it headless, verified 2026-09-10); on Windows 1.2.0 only the unscoped form read_file(*) matched — every scoped path form tested (drive-stripped dir, dir/*, full-drive, backslash) failed to authorize view_file, an upstream limitation to revisit on upgrades. grep_search/list_dir work unlisted. Production native settings carry read_file(*).",
      "verified_at": "2026-09-18",
      "version": "1.2.5"
    },
    "fs.write": {
      "status": "soft",
      "via": "RECALIBRATED 2026-09-10 on agy 1.2.0 (supersedes the 1.1.28 scoped-rule story AND the same-day intermediate 'unscoped-only' note, which was mechanism-confounded): write_to_file is artifact-confined upstream — a non-artifact target dies at permission conversion ('not a valid artifact path; artifacts must be in brain/<conversation>/', invalid_args) BEFORE any write_file rule is evaluated, so write_file allow rules are irrelevant to deliverable writes. Deliverables actually ride run_command (shell), gated by command(*); and run_command starts in agy's own scratch dir (~/.gemini/antigravity-cli/scratch, pwd-verified), NOT the run cwd — models must be told absolute paths (paidan injects a cwd hint into the task text, command.prompt_cwd_hint). SOFT because deliverable writes are shell-ridden and not path-governed; reliability depends on the model using absolute paths. read-only (--mode plan) produced no writes (P2 pass on 1.2.0). Evidence: 4-variant rule matrix + 3 raw-stream direct runs + brain transcript of run_20260910_ca92a967.",
      "verified_at": "2026-09-18",
      "version": "1.2.5"
    },
    "shell.exec": {
      "status": "soft",
      "via": "run_command auto-approved headless when the native allow rule command(*) is present (verified 2026-09-10: output captured, DONE, no denial); without the rule it auto-denies (request-review + no prompt channel). SOFT because shell is NOT path-confined without --sandbox: on 2026-09-10 a workspace-write run's shell leg wrote a duplicate deliverable into the paidan CALLER's cwd (outside the run cwd; run_command starts from an inherited PWD-ish directory on Windows — the suite documented this as 'run_command may start from C:\\'); write_file tool writes stay natively path-governed, shell writes are not. Under --sandbox run_command demands an unsandboxed grant headless runs can never receive — paidan does not pass --sandbox. 1.2.0 addendum: run_command starts in ~/.gemini/antigravity-cli/scratch regardless of the spawn cwd (pwd-verified 2026-09-10); 'context canceled' tool errors observed at turn end on otherwise-successful runs are upstream shell-leg teardown noise (1.1.28 and 1.2.0 alike).",
      "verified_at": "2026-09-10",
      "version": "1.2.0"
    },
    "net.fetch": {
      "status": "unverified",
      "via": "read_url_content tool exists and the native allow rule read_url(*) is in place, but no live fetch probe was run in the 2026-09-10 native matrix; 1.1.28 changed URL fetching to default-Ask (probe report 2026-09-09 §8), so expect auto-deny without the rule",
      "verified_at": "2026-09-10"
    },
    "interact.ask": {
      "status": "unsupported",
      "via": "headless has no approval channel: every ask/unlisted action is auto-denied and lands in result.denied_actions with the stderr jetski line (verified repeatedly 2026-09-10); ask write_file(*) is exactly this backstop",
      "verified_at": "2026-09-10"
    },
    "presets": {
      "read-only": "soft",
      "workspace-write": "supported",
      "unattended": "supported"
    },
    "presets_notes": "Mapping: read-only=--mode plan, workspace-write=--mode accept-edits, unattended=--mode accept-edits (+ native command(*) rule). 1.2.0 reality: write_to_file is artifact-confined upstream (brain/<conversation>/ only); deliverable writes ride run_command (shell, command(*) gated) whose start dir is agy's scratch (~/.gemini/antigravity-cli/scratch), so paidan appends a cwd/absolute-path hint to the task text (prompt_cwd_hint). Native settings are user-maintained; paidan never writes them (invariant 4). shell.exec and (since 1.2.0) fs.write are both soft: the shell leg is neither path-anchored nor path-governed; unattended submits carry a soft warning. [2026-09-11, agy 1.2.1: probe P2 came back indeterminate — the read-only probe run was cancelled before terminal evidence (no attributable refusal); plan-mode headless may now wait instead of auto-denying as it did on 1.1.28/1.2.0 (t2a). Demoted to soft pending a dedicated recheck. P1/P3 pass on 1.2.1.] [2026-09-18 1.2.5: P2 timed out at 90s and was cancelled by the probe; no attributable refusal. Read-only remains soft/unverified. Native help lists --sandbox, but this adapter does not inject or claim its enforcement.]",
    "denial_evidence": {
      "status": "supported",
      "via": "three in-band channels, all live-verified 2026-09-10: (1) result.denied_actions[] e.g. {action:write_file,display_name:WriteToFile} for ask/plan auto-denies; (2) step_update state=ERROR tool_info.error TOOL_ERROR messages ('permission check failed ... Matches user-configured deny rule', 'user denied permission ...') for explicit rules; (3) stderr 'jetski: no output produced — a tool required the \"<action>\" permission ... auto-denied'. Empty response with SUCCESS is a legal terminal shape and stays evidence, not automatic failure",
      "verified_at": "2026-09-10",
      "version": "1.2.0"
    },
    "default_mode": "workspace-write",
    "headless_notes": "Default accept-edits uses native allow rules (read_file(*) and command(*) on the verified Windows setup). paidan does not answer permission requests or broaden these rules. Missing rules need user-approved native setup. Enforcement remains soft where declared."
  },
  "resume": {
    "kind": "flag",
    "args": [
      "--conversation",
      "{session}"
    ],
    "cross_process": true,
    "notes": "Session handle = conversation_id (UUID) from the init/result events. Cross-process resume via --conversation <id> verified 2026-09-10 (1.2.0): recalled prior content exactly, and resolution is machine-wide — the same id resumed from a DIFFERENT cwd too (supersedes the 2026-09-09 probe report's cwd-scoped assumption for explicit ids). --continue/-c picks the most recent conversation of the current cwd (verified). mode_args are re-spliced on resume (no resume_argv): --mode and --conversation coexist in one argv (suite adapter shape; a resumed run re-tiers from the new invocation). Concurrent resume of the same conversation was not probed; treat as forbidden (transcript interleaving, claude-analog).",
    "verified_at": "2026-09-18",
    "version": "1.2.5"
  },
  "models": {
    "command": [
      "{bin}",
      "models"
    ],
    "parse": "agy-models",
    "notes": "Native agy models catalog. Authentication/account cannot be inferred from model IDs; connection remains unknown unless native config states it. Model IDs may encode tiers. Newer native --effort/model combinations are not yet supported by this adapter; follow native effort. Fixed selections bind to the current native metadata context; changed/unbound defaults require user choice. Current catalog membership is checked before fixed dispatch; credentials/quota remain unverified.",
    "bind_selection": true
  },
  "parser": "agy-print",
  "native_preflight": {
    "file": "{home}/.gemini/antigravity-cli/settings.json",
    "require_allow": [
      "read_file(*)",
      "command(*)"
    ],
    "notes": "Read-only check of the user-maintained native permission rules (2026-09-10/11 calibration). read_file(*) gates view_file (fs.read); command(*) gates run_command — which is what deliverable writes actually ride on agy 1.2.0 (write_to_file is confined to brain/<conversation>/ artifacts, so a write_file allow rule is irrelevant to deliverables and is deliberately NOT required; requiring it would push users to widen permissions for no effect — codex P1-12). Missing rules surface as a submit warning and a doctor status; paidan never writes this file (invariant 4)."
  },
  "parser_notes": "stdout NDJSON: init (conversation_id, cwd, permission_mode) -> step_update (agent_response text_delta accumulation; tool ERROR events carry permission TOOL_ERRORs) -> exactly one terminal result (status, response, conversation_id cross-checked against init, usage input/output/cache_read tokens, denied_actions). finalText = result.response, falling back to accumulated agent_response deltas with a warning when no result event arrives. Non-JSON lines, mid-line EOF and conflicting conversation ids degrade the parser. detectRefusals reads the stderr jetski auto-deny line and the print-timeout marker (partial output + exit 0 + SUCCESS; 1.2.0 timeout semantics). Evidence: live captures 2026-09-10 (1.1.x era probe set agy-native; raw scratch deleted 2026-09-13, verdicts distilled here) plus the same-day 1.2.0 recalibration runs, and the retired external-agent-suite agy adapter (archived 2026-09-12).",
  "capabilities": {
    "background_native": false,
    "cancel_native": false,
    "completed_nonzero_exit": false,
    "_capabilities_notes": "No native background for -p runs. Cancel = paidan taskkill tree (v0). Upstream >= 1.1.20 made exit codes semantic (non-zero = cascade failure); print-timeout and soft denials still exit 0 — terminal judgment is evidence-first, never exit-code-only."
  }
}
